name: Prettier

on:
  pull_request:
    branches: [main]

jobs:
  prettier-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Prettier
        run: npm run pretty
